export enum role {
    USER = "user",
    ADMIN = "admin",
    DOCTOR = "doctor"
}
export enum accessEnum {
    Create = "create",
    Update = "update",
    Delete = "delete"
}
export enum permissionEnum {
    Publication = "publication",
    Reports = "reports",
    DOCTOR = "doctor"
}