export enum StatusEnum {
    approve = "Approved",
    pending = "Pending",
    reject = "Rejected",
  }

  export enum ActionEnum {
    Create = 'Create',
    Update = 'Update',
    Delete = 'Delete',
  }