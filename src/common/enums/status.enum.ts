export enum StatusEnum {
    accept = "Accepted",
    pending = "Pending",
    reject = "Rejected",
  }

  export enum ActionEnum {
    Create = 'Create',
    Update = 'Update',
    Delete = 'Delete',
  }