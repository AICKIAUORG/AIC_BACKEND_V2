export enum StatusEnum {
    accept = "Accept",
    pending = "Pending",
    reject = "Rejected",
  }

  export enum ActionEnum {
    Create = 'Create',
    Update = 'Update',
    Delete = 'Delete',
  }